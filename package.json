{
  "name": "focus-time",
  "version": "0.7.0",
  "license": "MIT",
  "description": "Simple pomodoro app for KaiOS",
  "main": "index.html",
  "scripts": {
    "start": "parcel index.html --out-dir build && cp ./manifest.webapp ./build/ && cp -r ./assets/ ./build/",
    "build": "parcel build index.html --out-dir build && cp ./manifest.webapp ./build/ && cp -r ./assets/ ./build/",
    "test": "standard"
  },
  "devDependencies": {
    "@babel/core": "^7.11.6",
    "parcel-bundler": "^1.12.4",
    "standard": "^14.3.4"
  }
}